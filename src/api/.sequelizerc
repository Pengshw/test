const path = require('path');

module.exports = {
  'config': path.resolve('./app/config', 'migration.json'),
  'models-path': path.resolve('app', 'model'),
  'migrations-path': path.resolve('app', 'migrations')
}